server:
  port: 8888
spring:
  application:
    name: curso-springboot-3
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    open-in-view: false

springdoc:
  pathsToMatch: /api/v1/**
  swagger-ui:
    use-root-path: true
